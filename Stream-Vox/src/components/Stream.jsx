import Nav from './Nav'

const Stream = () => {
  return (
    <div>
      <header>
        <h1>STREAM FLOW</h1>
      </header>
      <main>
        {/* <p> */}
        Schedule a Stream:
        {/* </p> */}
      </main>
    </div>
  )
}

export default Stream

// IF statement for input submissions
